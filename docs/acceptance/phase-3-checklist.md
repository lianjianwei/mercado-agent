# Phase 3 侵权检测验收记录

验收日期：2026-08-27（Asia/Shanghai）

## 自动化验证

- [x] `npm test` 全量测试通过：33 个测试文件、169 项测试。
- [x] `npm run typecheck` 与 `npm run lint` 通过。
- [x] `npm run package` 正式打包通过。
- [x] `npm run test:e2e:run -- tests/e2e/infringement-flow.spec.ts` 通过同步→侵权检测→高风险结论→历史保留→重启持久化验证：1 项通过。
- [x] `npm run test:e2e:run` 全量 e2e 通过（工作台同步、侵权检测、配置持久化）：3 项通过。
- [x] 占位与泄密扫描通过：真实 App Key/Secret 未出现在 Git 跟踪或未跟踪文件中，无待替换占位；e2e 使用的测试密钥仅存在于测试文件。

## 本地受限品牌与风险规则

- [x] 99 个受限品牌全部解析，大小写/标点不敏感匹配，别名展开（`AP`/Audemars Piguet、`CK`/Calvin Klein、`YSL`/Yves Saint Laurent）。
- [x] 风险指纹：canonical JSON SHA-256；标题、品牌、类目、属性、SKU 名、图片变化会改变指纹；非风险字段变化不改变。
- [x] 确定性本地规则：品牌本体高风险；Generic 兼容配件不因品牌词直接判高并转 AI 复核；仿冒/复刻表述提高风险；官方授权暗示标记复核；服装敏感类目命中受限品牌判高。

## 多模态检测引擎

- [x] 三种 provider（豆包/DeepSeek/OpenAI）统一 OpenAI 兼容图文请求契约：`image_url` content 块、401/非 OK 映射安全错误、坏 JSON 抛结构化错误、取消传播。
- [x] DeepSeek 通过 `deepseek-v4-flash-vision-exp` 支持图片理解；非视觉模型传图由服务端返回明确错误并如实提示。
- [x] 引擎先跑本地规则：确定品牌本体高风险时不调用 AI，AI 不能静默降级；否则调用 AI 并 Zod 校验结构化输出。
- [x] 提示词要求区分品牌本体/兼容配件，并引用可见图片证据。

## 不可变检测历史

- [x] `infringement_runs` 只插入不覆盖；按插入序（rowid）取最新完成 run 作为当前结果。
- [x] 指纹不变时复用现有结果不产生新 run；指纹变化时版本递增（V1→V2），旧版本永久保留。
- [x] 批量检测单品失败不终止批次，逐商品报告失败。

## 工作台人工复核

- [ ] 在真实应用中用迪士尼品牌本体商品做检测，确认判定为高风险。
- [ ] 用 Generic 拓竹/Bambu Lab 兼容配件做检测，确认不因品牌词直接判高，AI 可读图片证据。
- [ ] 用普通无品牌商品做检测，确认 AI 判定并显示无/低风险。
- [ ] 用服装商品做检测，确认敏感类目规则生效。
- [ ] 修改标题或图片后复检，确认生成 V2 且 V1 标记已过期但保留。
- [ ] 双击/查看商品确认只读展示，无编辑或发布入口。
- [ ] 继续编辑按钮只记录用户选择，不改变风险结论。
- [ ] UI、日志、测试输出和 Git diff 中均不出现妙手 App Secret。

## 同步体验与状态模型精简（Phase 3.5 追加）

- [x] 同步全部只拉取妙手未发布商品，不再遍历定时发布/已发布历史/远端缺失；每商品仍拉详情并写快照。
- [x] 同步进度以终端式滚动日志实时展示（第几页、每条商品、失败原因、重试、耗时）。
- [x] 网络/超时/限流类错误详情拉取自动重试 1 次；业务错误不重试直接记录。
- [x] 顶部页签精简为「未发布」「本地已发布」；移除定时发布/已发布历史/远端缺失/全部记录。
- [x] 表格新增「本地发布」列（未发布/本地已发布/发布失败）；字段与本地发布时间已落库，发布接口接入后启用 2 小时规则。
- [x] 新增「清理数据」按钮：清空 products、product_snapshots、infringement_runs，保留凭证/代理/模型配置。
- [x] `npm test` 全量通过：34 个测试文件、200 项测试；typecheck 与 lint 干净。
- [x] `npm run test:e2e:run` 全量 e2e 通过：3 项（含同步日志面板、本地发布列、清理数据）。

## 阶段结论

- [ ] 用户明确确认“阶段 3 通过”。
